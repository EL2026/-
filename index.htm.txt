<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>スケジュール表</title>
<style>
* { box-sizing: border-box; margin: 0; padding: 0; }
body {
  font-family: -apple-system, BlinkMacSystemFont, "Hiragino Sans", "Yu Gothic UI", sans-serif;
  background: #f0f2f5; color: #1a1a1a; min-height: 100vh;
}
.top-bar {
  background: #1a56a0; color: #fff;
  padding: 0 2rem; height: 54px;
  display: flex; align-items: center; gap: 10px;
}
.top-bar .logo { font-size: 13px; font-weight: 700; opacity: .8; }
.top-bar .sep  { opacity: .3; }
.top-bar .ttl  { font-size: 16px; font-weight: 600; }
#cfg-banner {
  background: #fffbe6; border-bottom: 2px solid #f5c518;
  padding: 1.25rem 2rem;
}
#cfg-banner h3 { font-size: 14px; font-weight: 700; color: #7a5c00; margin-bottom: 6px; }
#cfg-banner p  { font-size: 13px; color: #6a5200; margin-bottom: 12px; line-height: 1.65; }
.cr { display: flex; gap: 8px; align-items: center; margin-bottom: 8px; flex-wrap: wrap; }
.cr label { font-size: 13px; color: #5a4a00; width: 160px; flex-shrink: 0; }
.cr input {
  flex:1; min-width:240px; max-width:420px;
  padding:7px 11px; font-size:13px; font-family:monospace;
  border:1px solid #ddd; border-radius:6px; background:#fff;
}
.cr input:focus { outline:2px solid #1a73e8; border-color:transparent; }
#sv-btn {
  margin-top:6px; padding:8px 22px;
  background:#1a73e8; color:#fff; border:none;
  border-radius:6px; font-size:14px; font-weight:600; cursor:pointer;
}
#sv-btn:hover { background:#1558b0; }
.main { max-width:980px; margin:0 auto; padding:1.75rem 1.5rem; }
.rec-meta {
  background:#fff; border-radius:10px; border:1px solid #e0e0e0;
  padding:14px 20px; display:flex; gap:16px; align-items:center; flex-wrap:wrap;
  margin-bottom:1.5rem;
}
.rec-meta .badge {
  background:#e8f0fe; color:#1a56a0;
  font-size:11px; font-weight:700; padding:3px 10px; border-radius:99px;
}
.rec-meta .title { font-size:18px; font-weight:700; }
.rec-meta .sub   { font-size:13px; color:#888; }
.section {
  background:#fff; border-radius:10px; border:1px solid #e0e0e0;
  overflow:hidden; margin-bottom:1.5rem;
}
.sec-hd {
  background:#f7f9fc; border-bottom:1px solid #e8edf2;
  padding:12px 20px; display:flex; align-items:center; gap:10px;
}
.sec-hd h2 { font-size:14px; font-weight:700; color:#333; }
.sec-hd .cnt {
  background:#1a56a0; color:#fff;
  font-size:11px; font-weight:700; padding:2px 9px; border-radius:99px;
}
table { width:100%; border-collapse:collapse; }
th {
  background:#f7f9fc; font-size:11px; font-weight:700; color:#888;
  text-align:left; padding:8px 16px; border-bottom:1px solid #eee;
  text-transform:uppercase; letter-spacing:.04em;
}
td { padding:11px 16px; border-bottom:1px solid #f2f2f2; font-size:13px; vertical-align:middle; }
tr:last-child td { border-bottom:none; }
tr:hover td { background:#fafcff; }
.fnc { display:flex; align-items:center; gap:10px; }
.xi {
  width:28px; height:28px; flex-shrink:0;
  background:#e8f5e9; border-radius:5px;
  display:flex; align-items:center; justify-content:center;
}
.xi svg { width:15px; height:15px; stroke:#2e7d32; }
.fn { font-weight:600; }
.fsz { color:#99